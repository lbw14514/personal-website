c:\wwwroot\www.wuyulbw.top_14514
1. Node.js 环境（用于 hakimi 桥接服务 bridge.js）
Node.js：v20.18.3（LTS）

npm：随 Node.js 安装（v10.8.2）

必需的 npm 包：

express：^4.18.2

uuid：8.3.2（CommonJS 兼容版本）

cors：^2.8.5

2. Python 环境（用于 Flask 后端 app.py，提供 /api/ip-info、/api/downloads、/api/download/、/api/mc-status）
Python：3.8 或更高（推荐 3.10+）

必需的 pip 包：

flask：2.x

mcstatus：^11.0 或更高

geoip2：^4.8（可选，若使用本地 MaxMind 数据库）

requests：^2.31.0

3. C++ 编译工具（用于生成 hakimi_engine.exe）
编译器：MinGW-w64（GCC 16.1.0，静态链接 -static）

C++ 标准：C++11

nginx 1.20+

端口占用
14514：HTTPS（Nginx 监听）

14513：HTTP 重定向（若配置了）

3000：Node.js 桥接服务（仅本地 127.0.0.1）

5000：Flask 后端（仅本地 127.0.0.1）

uuid 必须使用 8.3.2（因为新版本是 ESM 格式，Node 端 require 会报错）。

C++ 程序编译时建议添加 -static 标志，避免运行时缺少 libstdc++-6.dll。

Python 的 mcstatus 需要网络访问权限，用于查询外部 Minecraft 服务器。